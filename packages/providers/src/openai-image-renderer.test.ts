import { describe, expect, it } from "vitest";

describe("legacy model-provider image renderer", () => {
  it("has been removed from the canonical runtime", () => {
    expect(true).toBe(true);
  });
});
